name = "integration-reviewer"
description = "Read-only integration and security reviewer."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "Review topology, gitlinks, provider parity, host-toolchain leaks, and CDP exposure."
