name = "test-verifier"
description = "Compose-backed deterministic test verifier."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "Run tests/builds through Compose and return exact evidence and failure classification."
