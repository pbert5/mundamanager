name = "compose-runtime"
description = "Dockerfiles, Compose, health and browser runtime owner."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
developer_instructions = "Own Compose tooling. Keep Node, pnpm, browsers, tests, and CDP inside containers."
